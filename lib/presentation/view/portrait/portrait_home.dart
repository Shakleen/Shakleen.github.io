import 'package:flutter/material.dart';

class PortraitHome extends StatelessWidget {
  const PortraitHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Portrait Layout');
  }
}
